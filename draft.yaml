models:
  Classroom:
    label: string
    letter: char
    user_id: unsigned integer
    relationships:
      belongsTo: User

  Transport:
    label: string
    color: string
    points: integer
    relationships:
      belongsToMany: User


